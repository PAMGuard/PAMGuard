package loggerForms.actions;

/**
 * Owner of a logger action. doesn't do anything, but makes it easier to 
 * use the correct owner class when programming, so less likely to make mistakes. 
 */
public interface ActionOwner {

}
