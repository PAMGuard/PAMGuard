package PamguardMVC;

/**
 * Used as a flag to indicate that the data unit implementing this interface holds
 * some type of acoustic data.  This interface is empty and does not require any
 * methods to be implemented which are not already in PamDataUnit, however you must ensure that
 * getStartSample() returns a non null number
 * 
 * @author Doug Gillespie
 *
 */
public interface AcousticDataUnit {

	
}
