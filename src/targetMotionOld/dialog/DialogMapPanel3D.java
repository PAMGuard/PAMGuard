package targetMotionOld.dialog;

import Map.MapController;
import Map.MapPanel;
import Map.SimpleMap;

/**
 * Modified version of the map panel so that some of it's drawing can be enhanced / reduced
 * to suit the needs ot the target motion dialog. 
 * @author dg50
 *
 */
public class DialogMapPanel3D extends MapPanel {

	public DialogMapPanel3D(MapController mapController, SimpleMap simpleMap) {
		super(mapController, simpleMap);
	}

}
